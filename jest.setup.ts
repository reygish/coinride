/**
 * jest.setup.ts
 * File setup yang dijalankan sebelum setiap test suite.
 * Import jest-dom untuk mendapatkan matcher custom seperti toBeInTheDocument(), etc.
 */

import "@testing-library/jest-dom";
